import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fabyhouse.com',
  appName: 'fabyhousecreation',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
